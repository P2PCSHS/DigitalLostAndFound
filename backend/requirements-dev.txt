# Development and CI dependencies. Installs the runtime set as well, so the
# production image can build from requirements.txt alone.
-r requirements.txt

iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
ruff==0.16.8
